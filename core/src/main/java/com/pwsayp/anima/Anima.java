package com.pwsayp.anima;

import net.minecraftforge.fml.common.Mod;
import net.minecraftforge.fml.javafmlmod.FMLJavaModLoadingContext;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

/**
 * Anima Core — общее основание модов семейства.
 *
 * <p>Своего в игре не добавляет ничего: ни мобов, ни блоков, ни предметов. Здесь живёт
 * только то, что иначе пришлось бы переписывать в каждом моде — утилиты работы с миром,
 * каркас конфигов, общий экран настроек, общие теги и шина мировых событий.</p>
 *
 * <p>Ядро — обязательная зависимость каждого мода и ставится отдельным файлом: внутрь
 * модов оно не вкладывается. Отсюда правило, которое держит всю затею: <b>мод знает про
 * ядро, но никогда про соседний мод</b>. Взаимодействуют моды только через шину — один
 * шлёт сигнал, а кто на него отзовётся, его не касается. Поэтому любой мод работает в
 * одиночку, а вместе они складываются в общий живой мир.</p>
 *
 * <p>Отсюда же дисциплина версий: внутри мажорной версии ядро только дополняется. Ничего
 * не переименовывается и не удаляется — иначе у игроков соберётся зоопарк из старых модов
 * и нового ядра.</p>
 */
@Mod(Anima.MODID)
public final class Anima {
    public static final String MODID = "anima";

    public static final Logger LOG = LoggerFactory.getLogger("Anima");

    public Anima(final FMLJavaModLoadingContext context) {
        // Пока пусто: ядро появляется в репозитории раньше, чем то, что в него переедет.
    }
}
